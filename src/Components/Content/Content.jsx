import React from 'react';
import s from './Content.module.scss'

const Content = () => {
    return (
        <div className={s.mainBlock}>
            <h1 className={s.header1}>Hello!</h1>
        </div>
    );
};

export default Content;